query CartDeliveryOptionsTransformRunInput {
  deliveryCustomization {
    metafield(namespace: "$app:fulfillment-selector", key: "function-configuration") {
      jsonValue
    }
  }
  cart {
    deliveryGroups {
      deliveryOptions {
        code
        title
        description
        handle
        cost {
          amount
        }
      }
    }
    fulfillmentTypeAttribute: attribute(key: "fulfillmentType") {
      value
    }
    pickupLocationIdAttribute: attribute(key: "pickupLocationId") {
      value
    }
  }
}
